import NavBar from '@/components/shared/navBar';
import React from 'react';

const page = () => {
    return (
        <div>
            <NavBar></NavBar>
            this is calender scheduling page.
        </div>
    );
};

export default page;